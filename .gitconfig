[user]
	name = Avinash Krishnan
	email = avinashkris9@gmail.com
# [core]
# 	autocrlf = true
[branch]

# Show most recently changed branches first.
sort = -committerdate
[merge]

# Include summaries of merged commits in newly created merge commit messages
log = true

[core]

exlcludesfile=~/.gitignore

[coloum]
ui=auto

[rebase]
updateRefs=true

[alias]

fpush = "push --force-with-lease"
which=!git branch | grep -i
lucky= !git checkout $(git which $i -m1)
